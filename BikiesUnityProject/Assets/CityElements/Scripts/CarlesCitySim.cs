using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarlesCitySim : MonoBehaviour
{
    [Header("City Simulation")]
    public List<BikeStation> cityBikeStations = new List<BikeStation>();
    //public List<Apartment> nearbyApartments = new List<Apartment>();
    //public List<InterestPoint> nearbyInterestPoints = new List<InterestPoint>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
